# AE tải về xong xóa hết pkl, png, json đi còn chừa lại py và requirements.txt thôi 
# Step 1: create vitrual env

python -m venv venv
venv\Scripts\activate

# Step2: download libs
pip install -r requirements.txt

=> run 
